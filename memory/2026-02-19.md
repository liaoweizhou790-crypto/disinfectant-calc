# 2026-02-19 工作日志

## 📝 待办事项：多代理路由方案

### 背景
当前只有一个 `main` Agent 处理所有任务，需要配置多个独立 Agent 分别处理不同工作场景。

### 目标
- 实现多个 Agent 独立运行
- 不同工作场景路由到对应 Agent
- 保持工作空间和记忆的隔离

---

## ✅ 待办清单

### Phase 1: 基础配置
- [ ] 创建 `cdc` Agent（疾控业务）
  - 工作空间：`~/.openclaw/workspace-cdc`
  - 复制现有 `SOUL.md`（专业严谨风格）
  - 复制 `USER.md`（廖所长信息）
  - 配置 `AGENTS.md`（疾控业务规则）
  
- [ ] 创建 `dev` Agent（开发工作）
  - 工作空间：`~/.openclaw/workspace-dev`
  - 配置独立的环境和工具权限

### Phase 2: 路由配置
- [ ] 配置 `~/.openclaw/openclaw.json` 多代理路由
  ```json5
  {
    agents: {
      list: [
        { id: "main", default: true, workspace: "~/.openclaw/workspace" },
        { id: "cdc", workspace: "~/.openclaw/workspace-cdc" },
        { id: "dev", workspace: "~/.openclaw/workspace-dev" }
      ]
    },
    bindings: [
      // 根据渠道/账号路由
      { agentId: "main", match: { channel: "webchat" } },
      { agentId: "cdc", match: { channel: "feishu", accountId: "work" } },
      { agentId: "dev", match: { channel: "telegram" } }
    ]
  }
  ```

### Phase 3: 渠道对接
- [ ] 飞书渠道配置
  - 企业飞书账号 → `cdc` Agent
  - 个人飞书账号 → `main` Agent
  
- [ ] Telegram 渠道配置（可选）
  - Telegram → `dev` Agent

### Phase 4: 测试验证
- [ ] 测试不同渠道路由是否正确
- [ ] 验证 Agent 间记忆隔离
- [ ] 验证工具权限独立配置

---

## 📋 参考命令

```bash
# 添加新 Agent
openclaw agents add cdc

# 查看所有 Agent
openclaw agents list --bindings

# 重启 Gateway 生效
openclaw gateway restart

# 连接到指定 Agent 的 TUI
openclaw tui --agent cdc
```

---

## 🔗 参考文档

- OpenClaw 多代理路由：https://docs.openclaw.ai/concepts/multi-agent
- 绑定规则：https://docs.openclaw.ai/concepts/multi-agent#routing-rules
- 每 Agent 沙箱配置：https://docs.openclaw.ai/tools/multi-agent-sandbox-tools

---

## 💡 优先级

**高优先级：**
1. 创建 `cdc` Agent（疾控业务独立）
2. 配置飞书路由规则

**中优先级：**
3. 创建 `dev` Agent
4. 配置 Telegram 渠道

**低优先级：**
5. 群组 @提及切换
6. Agent 间通信测试

---

## 备注

- 当前所有工作都在 `main` Agent 完成
- 迁移时需复制关键配置文件（SOUL.md, USER.md, TOOLS.md）
- 历史记忆可选择性迁移或保留在 main Agent
- 消毒剂计算器项目文件需要确定归属（建议放在 cdc Agent 工作空间）
